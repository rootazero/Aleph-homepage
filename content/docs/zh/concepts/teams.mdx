---
title: "团队"
description: "具有 SQLite 支持存储的代理团队管理，用于管理代理团队、成员资格、任务跟踪和生命周期管理。"
---

`teams` 模块提供类型和 SQLite 支持存储，用于管理代理团队。它处理团队创建、成员资格、每团队任务跟踪、生命周期管理和计划审批工作流。

## 设计理念

1. **持久优先** — 所有团队状态都存储在 SQLite 中以实现持久性
2. **生命周期感知** — 团队具有显式状态（活跃、解散、归档）
3. **任务集成** — 团队可以具有带有每成员分配的共享任务

---

## 核心类型

### Team

```rust
pub struct Team {
    pub id: TeamId,
    pub name: String,
    pub description: String,
    pub status: TeamStatus,
    pub members: Vec<TeamMember>,
    pub created_at: DateTime<Utc>,
}
```

### TeamStatus

```rust
pub enum TeamStatus {
    Active,
    Disbanded,
    Archived,
}
```

### TeamMember

```rust
pub struct TeamMember {
    pub id: MemberId,
    pub name: String,
    pub role: TeamRole,
    pub status: MemberStatus,
}

pub enum TeamRole {
    Leader,
    Member,
    Observer,
}
```

### TeamTask

```rust
pub struct TeamTask {
    pub id: TaskId,
    pub title: String,
    pub assignee: Option<MemberId>,
    pub status: TeamTaskStatus,
}

pub enum TeamTaskStatus {
    Pending,
    InProgress,
    Completed,
    Blocked,
}
```

---

## TeamStore

SQLite 支持的持久化：

```rust
pub trait TeamStore: Send + Sync {
    async fn create_team(
        &self,
        team: &NewTeam,
    ) -> Result<TeamId>;

    async fn add_member(
        &self,
        team_id: TeamId,
        member: &NewTeamMember,
    ) -> Result<()>;

    async fn get_team(
        &self,
        team_id: TeamId,
    ) -> Result<Option<Team>>;

    async fn list_teams(
        &self,
    ) -> Result<Vec<TeamSummary>>;

    async fn create_task(
        &self,
        team_id: TeamId,
        task: &NewTeamTask,
    ) -> Result<TaskId>;
}
```

### SqliteTeamStore

```rust
pub struct SqliteTeamStore {
    pool: SqlitePool,
}
```

**安全：**
- 所有查询使用参数化 `params![]`
- `TeamStatus` 和 `TeamTaskStatus` 实现 `FromSql` 以进行适当的错误传播（无 `unwrap_or_default`）
- 使用 `tokio::sync::Mutex`（无中毒）

---

## 生命周期

```
已创建 → 活跃 → [已解散 | 已归档]
```

**转换：**
- `已创建 → 活跃` — 团队已准备好使用
- `活跃 → 已解散` — 团队已解散（成员已移除）
- `活跃 → 已归档` — 团队已归档（只读）

**验证：** 操作在继续之前检查团队状态。向已解散的团队添加成员会返回错误。

---

## 事件

```rust
pub enum TeamEvent {
    TeamCreated { team_id: TeamId },
    MemberJoined { team_id: TeamId, member_id: MemberId },
    MemberLeft { team_id: TeamId, member_id: MemberId },
    TaskCreated { team_id: TeamId, task_id: TaskId },
    TaskCompleted { team_id: TeamId, task_id: TaskId },
}
```

---

## 工件

团队可以生成共享工件：

```rust
pub struct TeamArtifact {
    pub id: ArtifactId,
    pub name: String,
    pub content_type: String,
    pub content: Vec<u8>,
}
```

---

## 安全属性

- **无 SQL 注入** — 整个参数化查询
- **无锁中毒** — 使用 `tokio::sync::Mutex`
- **类型安全状态** — `FromSql` 实现防止静默解析失败
- **生产中无 unwrap** — 所有数据库操作返回 `Result`

---

## 代码位置

- `src/teams/mod.rs` — 模块入口点
- `src/teams/store.rs` — `TeamStore` trait 和 SQLite 实现
- `src/teams/types.rs` — 核心类型
- `src/teams/lifecycle.rs` — 生命周期管理
- `src/teams/events.rs` — 事件定义
- `src/teams/artifacts.rs` — 工件管理
- `src/teams/plans.rs` — 计划审批工作流
- `src/teams/context.rs` — 代理的团队上下文
- `src/teams/sessions/` — 会话管理
- `src/teams/messages/` — 团队消息

## 另请参阅

- [代理运行时](/docs/zh/concepts/agent-runtime) — 代理如何生成和管理任务
- [群聊](/docs/zh/concepts/group-chat) — 多人格讨论
- [竞技场](/docs/zh/concepts/arena) — 多代理协作工作区
