---
title: "执行器"
description: "工具执行引擎、3 种处理器来源、分层中间件与超时管理。"
---

# 执行器

执行器是工具执行引擎。它接收来自思考器（通过调度器）的工具决策，并将其路由到相应的处理器。

**位置**：`src/engine/`

---

## 执行流程

```
Thinker Decision (tool_use)
     │
     ▼
Dispatcher
     │
     ▼
Executor
     │
     ├───▶ Builtin Tool (AlephTool trait)
     │       Direct Rust execution
     │
     ├───▶ MCP Tool (Model Context Protocol)
     │       JSON-RPC to external process
     │
     └───▶ Extension Tool (WASM / Node.js)
             Plugin runtime execution
```

Exec 类工具（`code_exec`、`bash_exec`）在工具与进程执行之间通过额外的 **Sandbox 层**（`src/sandbox/`）路由。Sandbox 负责每个会话的工作空间配置、能力执行以及通过 `OsSandboxDriver` 进行操作系统级别的 seatbelt 隔离。

---

## ToolService 外观

消费者仅依赖 `Arc<dyn ToolService>`：

```rust
pub trait ToolService: Send + Sync + 'static {
    async fn execute(&self, name: &str, input: Value) -> Result<ToolOutput, ToolError>;
    async fn list(&self) -> Vec<ToolDefinition>;
    async fn describe(&self, name: &str) -> Option<ToolDefinition>;
}
```

---

## 中间件栈

```
ExecAuditLayer → PermissionLayer → ContextRuleLayer → TimeoutLayer → CoreDispatch
```

| Layer | Purpose |
|-------|---------|
| `ExecAuditLayer` | Audit logging |
| `PermissionLayer` | Permission checks (`SmartFilter`) |
| `ContextRuleLayer` | Context-based rules |
| `TimeoutLayer` | Timeout enforcement |
| `CoreDispatch` | Innermost layer holding the `ToolRegistry` with `BuiltinHandler`, `McpHandler`, and `ExtensionHandler` |

`CoreDispatch` 持有一个由 `ArcSwap` 支持的 `ToolRegistry`，包含三种处理器来源：
- `BuiltinHandler`
- `McpHandler`
- `ExtensionHandler`

---

## 核心分派

工具作者实现 `AlephTool` —— 外观通过 `BuiltinHandler` 适配它们。无需作者侧 API 变更。

会话事件追踪：调用者通过 `crate::session::invoke_with_session_trace(tool_svc, session_svc, ...)` 将 `ToolService` 分派与 `SessionService` 事件发射捆绑在一起。

---

## AlephTool Trait

### 静态分派（编译时）

```rust
pub trait AlephTool: Clone + Send + Sync + 'static {
    /// Tool name (used in LLM tool_use)
    const NAME: &'static str;

    /// Tool description for LLM
    const DESCRIPTION: &'static str;

    /// Argument type (auto JSON Schema via schemars)
    type Args: Serialize + DeserializeOwned + JsonSchema + Send;

    /// Return type
    type Output: Serialize + Send;

    /// Execute the tool
    async fn call(&self, args: Self::Args) -> Result<Self::Output>;
}
```

### 动态分派（运行时）

```rust
pub trait AlephToolDyn: Send + Sync {
    fn name(&self) -> &str;
    fn definition(&self) -> ToolDefinition;
    fn call(&self, args: Value) -> BoxFuture<'_, Result<Value>>;
}

// Blanket impl: Any AlephTool is also AlephToolDyn
impl<T: AlephTool> AlephToolDyn for T { // Blanket implementation }
```

---

## Exec 类工具与 Sandbox

- `code_exec`、`bash_exec` 通过 Sandbox 层路由
- `Sandbox` trait → `WorkspaceSandbox` → `OsSandboxDriver`
- 每个会话的工作空间位于 `~/.aleph/workspaces/{hash(session_id)}/`
- 工具级权限（`SmartFilter`）控制调用*是否*运行
- Sandbox 的能力检查控制子进程一旦获准运行后*能做什么*

---

## 另请参阅

- [Tool Architecture](./tools) — 工具开发指南
- Sandbox — 执行隔离
- MCP — Model Context Protocol
